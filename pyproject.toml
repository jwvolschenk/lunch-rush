[project]
name = "lunch-rush"
version = "0.1.0"
description = "A tiny helper for scheduling team lunch orders."
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["lunch_rush*"]
